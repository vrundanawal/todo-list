import React from 'react'

const ToggleButton = ({title, onToggle, children}) => {
    
  return (
    <div>
        <button>Add</button>
        <button>Remove</button>
    </div>
  )
}

export default ToggleButton